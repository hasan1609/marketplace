import 'package:flutter/material.dart';

class HalGame extends StatelessWidget {
  const HalGame({Key? key}) : super(key: key);

  @override
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text("ini halaaman game")],
      ),
    );
  }
}
