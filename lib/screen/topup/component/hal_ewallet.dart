import 'package:flutter/material.dart';

class HalEwallet extends StatelessWidget {
  const HalEwallet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text("ini halaaman ewallet")],
      ),
    );
  }
}
